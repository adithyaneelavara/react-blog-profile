version: 0.1

phases:
  build:
    commands:
      - echo Nothing to do yet
      - npm install
      - npm run build

artifacts:
  files:
    - index.html
    - blog.html
    - favicon.ico
    - AdithyaNeelavara-Resume.pdf
    - images/*
    - styles/*
    - js/*
    - dist/*